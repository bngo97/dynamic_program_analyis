program P
	const int a = 10;
	const char b = 'b', c = 'c', d = 10;
	enum e {
		a,
		b=10,
		c,
		d=20
	}
	enum e2 {
		e,f
	}
	int g, h;
	char i, j;
	class a extends b implements c, d {
		int a, b;
		{
			void add(int a, int b) int c, d; {
				//a.b.c[new x[z+1]] = q+111;
				//if(x > 0) x++;
				//for(x=0;x<10;x=z(f, g)) y = x + 1;		
				//break;
				//continue;	
				//return x+1-5*9;
				//read(x);
				//print(x, 5);
				if(x) {
					continue;
					break;
					x++;
				}
			}
		}
	}
{
	
	int sub(char c, Object o) bool b; {
		for(;;) {
			break;
			continue;
			y--;
		}
	}

	void main() int a, b; {}

	void main() {}

} 